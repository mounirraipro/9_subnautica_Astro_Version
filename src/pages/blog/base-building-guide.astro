---
import BlogArticle from "@/components/BlogArticle.astro";
import { articles } from "@/data/articles";

const article = articles.find((item) => item.slug === "base-building-guide");
if (!article) throw new Error("Missing article: base-building-guide");
---

<BlogArticle article={article} />
