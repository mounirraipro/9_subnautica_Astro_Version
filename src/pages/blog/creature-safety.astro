---
import BlogArticle from "@/components/BlogArticle.astro";
import { articles } from "@/data/articles";

const article = articles.find((item) => item.slug === "creature-safety");
if (!article) throw new Error("Missing article: creature-safety");
---

<BlogArticle article={article} />
